# Security and Responsible Disclosure

This project processes public mathematical source code and produces local logs. It does not require secrets or API keys.

Before running scripts, inspect them, use an isolated development environment, avoid privileged credentials, remove private data from logs, and pin the reviewed commit and dependencies.

For vulnerabilities in this benchmark’s scripts, open a minimal report without secrets. For vulnerabilities in upstream tools or the evaluated repository, follow the relevant upstream disclosure process.

